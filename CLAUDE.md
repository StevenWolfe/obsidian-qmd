# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

`obsidian-qmd` — an Obsidian plugin adding support for Quarto Markdown (`.qmd`) files. This file will be updated once the codebase is established.

## Commands

_To be filled in once the project is bootstrapped (e.g. `npm install`, `npm run build`, `npm test`)._

## Architecture

_To be filled in once the initial structure is committed._
